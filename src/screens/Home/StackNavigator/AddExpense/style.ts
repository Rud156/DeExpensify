import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  bottomView: {
    display: 'flex',
    flexDirection: 'row',
    flex: 1,
    position: 'absolute',
    bottom: 0,
  },
});

export default style;
