import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  button: {
    height: 40,
    flex: 1,
  },
  text: {
    flex: 1,
    fontSize: 12,
    textAlign: 'center',
  },
});

export default style;
