import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  button: {
    position: 'absolute',
    bottom: 14,
    height: 40,
  },
  text: {
    fontSize: 14,
  },
});

export default style;
